"""Bundled copies of the example plugins, written out by ``protobot setup``.

This package is data, not code to import: the files next to it are the plugin
sources, shipped so a pip-installed ProtoBot can still create a starter
plugins/ directory. The canonical sources live in the repository's plugins/
directory; release.py refreshes these copies at build time.
"""
